# Normalize line endings: let git decide on checkin, keep platform-native on checkout
* text=auto

# Scripts that must keep specific endings
*.ps1 text eol=crlf
*.sh  text eol=lf

# Treat as binary (no diff/merge)
*.dll binary
*.lib binary
*.so  binary
*.dylib binary
*.pdb binary
